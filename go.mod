// exi-go module - ISO 15118 EXI codec generator rewrite (Go)
// License: Apache-2.0 (see ../LICENSE)
//
// This module is the starting point for the pure-Go rewrite of the
// exi-go project. It will contain the generator, EXI runtime and
// bindings (C and Python) laid out under this repository's tree.
//
// NOTE: This file intentionally uses a neutral module path. Replace
// `example.com/exi-go` with your preferred repository path (e.g.
// github.com/<org>/exi-go) before publishing.
module example.com/exi-go

go 1.20

// No dependencies yet. Add `require` entries when external modules are needed.
